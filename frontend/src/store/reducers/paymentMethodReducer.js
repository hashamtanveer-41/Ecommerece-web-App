const initialState = {
   paymentMethod: null,
}

export const paymentMethodReducer = (state = initialState, action) => {
    switch (action.type) {
        case "":
        default:
            return state;
    }
};